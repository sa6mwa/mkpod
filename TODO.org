* TODO
- [X] Refactor cmd/mkpod/ to new mkpod under top-level directory using ports and adapters pattern
- [X] Implement the remove-remote-master from asset bucket feature
- [ ] Make full e2e integration tests
- [X] Ensure it is impossible to remove a remote master if the local master is not available
- [X] Top-level pubDate is zero date if not present at parsing, fix it, should be current time if zero
- [X] Make the e2e Makefile make-public target work
- [X] Author field is not filled in if it's empty on an episode, should take from top-level and fail if there is no top-level AND episode author
- [X] The explicit field does not seem to be filled in in the podcast.rss, should default to the top-level and always default to "no" if empty everywhere
- [X] When there is no output, duration, length, type, image, and author field in the atom, parsing should fail
- [ ] -ffmetadata.txt files remain under /tmp, have them removed when retrieved ffmetadata
- [X] mkpod does not detect that atom field in podspec is empty as there is probably no validator in the new mkpod resulting in no such file or directory error when trying to save the rss
- [ ] Add feature to use pubDate fields as "now", today, yesterday, HH:MM or HH:MM:SS or HHMM to set time today, or "yesterday HHMM", etc
